class Dssv {
    constructor() {
        this.dssv = [];
    }

    // liên quan tới mảng
    // thêm, xóa, sửa, tìm kiếm (danh sách, chi tiết)
    // param sv: đối tượng sinh viên
    themSV(sv) {
        this.dssv.push(sv);
    }
}